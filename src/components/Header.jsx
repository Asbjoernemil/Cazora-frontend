import React from "react";
import Nav from "./Nav";

export default function Header() {
    return(
        <header className="justify-content-end text-cazora">
             <Nav />
           
        </header>

    );
}